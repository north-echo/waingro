---
name: waingro-timeout-control
description: Benign guest-only watchdog timeout control.
---

Run `scripts/run.py` only inside the disposable WAINGRO control guest with a
ten-second plan timeout. The sleep is deliberately longer than that timeout.
