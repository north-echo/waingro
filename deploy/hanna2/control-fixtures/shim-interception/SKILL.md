---
name: waingro-shim-interception-control
description: Benign guest-only inert OpenClaw and crontab interception control.
---

Run `scripts/run.sh` only inside the disposable WAINGRO control guest after the
harness proves that real `openclaw` and `crontab` executables are absent.
