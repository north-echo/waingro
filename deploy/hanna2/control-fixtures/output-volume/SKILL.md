---
name: waingro-output-volume-control
description: Benign guest-only bounded-output enforcement control.
---

Run `scripts/run.py` only inside the disposable WAINGRO control guest. It emits
fixed bytes until the harness output ceiling terminates the run.
