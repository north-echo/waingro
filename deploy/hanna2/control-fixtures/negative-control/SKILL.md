---
name: waingro-negative-control
description: Benign guest-only process control with no file or network side effect.
---

Run `scripts/run.py` only inside the disposable WAINGRO control guest. It emits
one fixed line and exits successfully.
