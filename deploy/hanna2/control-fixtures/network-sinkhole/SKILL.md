---
name: waingro-network-sinkhole-control
description: Benign guest-only DNS, HTTPS, and fixed-response sinkhole control.
---

Run `scripts/run.py` only inside the disposable WAINGRO control guest with the
plan-bound `fixture.invalid` sinkhole response enabled.
